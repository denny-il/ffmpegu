{
  "name": "ffmpegu",
  "version": "0.1.0",
  "type": "module",
  "description": "Simple ffmpeg library for JavaScript backend runtimes with direct access to ffmpeg binary functionality",
  "keywords": [
    "ffmpeg",
    "video",
    "audio",
    "conversion",
    "nodejs",
    "bun",
    "deno",
    "stream",
    "backend"
  ],
  "author": "ilchishin.denis@gmail.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/denny-il/ffmpegu.git"
  },
  "scripts": {
    "build": "tsgo -p tsconfig.build.json",
    "dev": "tsgo --watch",
    "check": "$npm_execpath run check:fmt && $npm_execpath run check:lint && $npm_execpath run check:type",
    "check:fmt": "biome format",
    "check:lint": "biome lint",
    "check:type": "tsgo --noEmit",
    "test": "$npm_execpath run test:unit && $npm_execpath run test:integration && $npm_execpath run test:package",
    "test:integration": "vitest run tests/integration",
    "test:package": "$npm_execpath run build && vitest run tests/package",
    "test:unit": "vitest run tests/unit",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage",
    "lint": "biome lint",
    "format": "biome format --write",
    "format:check": "biome format",
    "docs:generate": "node --env-file .env scripts/ffmpeg-documentation.ts",
    "prepublish": "rm -rf ./dist && $npm_execpath run build && $npm_execpath run test",
    "version:git": "git add package.json && git commit -m 'chore: release v$npm_package_version' || echo 'No changes to commit' && git tag v$npm_package_version && git push && git push --tags",
    "version:publish": "$npm_execpath publish --access public",
    "version:minor": "$npm_execpath run prepublish && $npm_execpath version minor --no-git-tag-version && $npm_execpath run version:git && $npm_execpath run version:publish",
    "version:major": "$npm_execpath run prepublish && $npm_execpath version major --no-git-tag-version && $npm_execpath run version:git && $npm_execpath run version:publish",
    "version:patch": "$npm_execpath run prepublish && $npm_execpath version patch --no-git-tag-version && $npm_execpath run version:git && $npm_execpath run version:publish"
  },
  "devDependencies": {
    "@ai-sdk/google": "^3.0.53",
    "@biomejs/biome": "2.4.12",
    "@types/node": "^24.0.0",
    "@typescript/native-preview": "7.0.0-dev.20260416.2",
    "@vitest/coverage-v8": "4.0.18",
    "ai": "^6.0.141",
    "htmlparser2": "^12.0.0",
    "vitest": "4.1.2"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "os": [
    "darwin",
    "linux"
  ],
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "README.md",
    "docs/README.md",
    "docs/ffmpegu/*.md",
    "LICENSE"
  ],
  "packageManager": "pnpm@10.28.2+sha512.41872f037ad22f7348e3b1debbaf7e867cfd448f2726d9cf74c08f19507c31d2c8e7a11525b983febc2df640b5438dee6023ebb1f84ed43cc2d654d2bc326264"
}
